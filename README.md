# bot-exec

Execute a command

## Usage

```
Usage: exec [options] <command>

  Execute a command

  Options:

    --help      output usage information
    -p, --path  Path the command is executed in
```
